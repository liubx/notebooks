---
title: 告警规则说明
type: reference
tags:
  - type/需求管理
  - topic/告警
created: 2026-04-14
modified: 2026-04-14
---

# 告警规则说明

## 告警类型总览

平台支持 9 种告警规则，按触发机制可分为三大类：

| 分类 | 告警类型 | 触发对象 | 核心判断依据 |
|------|---------|---------|-------------|
| **围栏类** | 违规进入 | 定位对象 | 进入禁止区域 |
| | 违规离开 | 定位对象 | 离开限定区域 |
| | 超额 | 围栏区域 | 区域内人数超过上限 |
| | 缺额 | 围栏区域 | 区域内人数低于下限 |
| **时间类** | 滞留超时 | 定位对象 | 在某区域停留超过阈值 |
| | 暂离超时 | 定位对象 | 离开指定区域超过阈值 |
| | 手推车停滞超时 | 定位对象 | 手推车静止超过阈值 |
| **状态类** | 低电量 | 定位标签 | 电量低于阈值 |
| | 聚集 | 定位对象群体 | 短时间内多人聚集 |

---

## 围栏类告警

### 1. 违规进入

| 属性 | 说明 |
|------|------|
| 触发条件 | 定位对象进入设定的围栏区域 |
| 关联配置 | 围栏 + 作用范围（标签/分组过滤） |
| 告警生成 | 对象位置落入围栏多边形内时触发 |
| 告警关闭 | 对象离开围栏区域后自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B[计算对象与围栏位置关系]
    B --> C{对象在围栏内?}
    C -->|是| D{对象在作用范围内?<br>标签/分组匹配}
    D -->|是| E{该对象已有<br>此围栏的告警?}
    E -->|否| F[/🔔 生成违规进入告警/]
    E -->|是| G([保持告警，不重复生成])
    D -->|否| H([不在作用范围，跳过])
    C -->|否| I{该对象已有<br>此围栏的告警?}
    I -->|是| J[/✅ 关闭违规进入告警<br>转入历史告警/]
    I -->|否| K([无事发生])

    style F fill:#ff6b6b,color:#fff
    style J fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

### 2. 违规离开

| 属性 | 说明 |
|------|------|
| 触发条件 | 定位对象离开设定的围栏区域 |
| 关联配置 | 围栏 + 作用范围（标签/分组过滤） |
| 告警生成 | 对象位置离开围栏多边形时触发 |
| 告警关闭 | 对象回到围栏区域内后自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B[计算对象与围栏位置关系]
    B --> C{对象在围栏外?}
    C -->|是| D{对象在作用范围内?<br>标签/分组匹配}
    D -->|是| E{该对象已有<br>此围栏的告警?}
    E -->|否| F[/🔔 生成违规离开告警/]
    E -->|是| G([保持告警，不重复生成])
    D -->|否| H([不在作用范围，跳过])
    C -->|否| I{该对象已有<br>此围栏的告警?}
    I -->|是| J[/✅ 关闭违规离开告警<br>转入历史告警/]
    I -->|否| K([无事发生])

    style F fill:#ff6b6b,color:#fff
    style J fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

### 3. 超额

| 属性 | 说明 |
|------|------|
| 触发条件 | 围栏区域内定位对象数量超过设定上限 |
| 关联配置 | 围栏 + 人数上限阈值 |
| 告警生成 | 区域内对象计数 > 上限时触发 |
| 告警关闭 | 区域内对象计数 ≤ 上限时自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B[统计围栏内当前对象数量]
    B --> C{数量 > 人数上限?}
    C -->|是| D{该围栏已有<br>超额告警?}
    D -->|否| E[/🔔 生成超额告警<br>记录当前人数/]
    D -->|是| F([保持告警，不重复生成])
    C -->|否| G{该围栏已有<br>超额告警?}
    G -->|是| H[/✅ 关闭超额告警<br>转入历史告警/]
    G -->|否| I([无事发生])

    style E fill:#ff6b6b,color:#fff
    style H fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

### 4. 缺额

| 属性 | 说明 |
|------|------|
| 触发条件 | 围栏区域内定位对象数量低于设定下限 |
| 关联配置 | 围栏 + 人数下限阈值 |
| 告警生成 | 区域内对象计数 < 下限时触发 |
| 告警关闭 | 区域内对象计数 ≥ 下限时自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B[统计围栏内当前对象数量]
    B --> C{数量 < 人数下限?}
    C -->|是| D{该围栏已有<br>缺额告警?}
    D -->|否| E[/🔔 生成缺额告警<br>记录当前人数/]
    D -->|是| F([保持告警，不重复生成])
    C -->|否| G{该围栏已有<br>缺额告警?}
    G -->|是| H[/✅ 关闭缺额告警<br>转入历史告警/]
    G -->|否| I([无事发生])

    style E fill:#ff6b6b,color:#fff
    style H fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

## 时间类告警

### 5. 滞留超时

| 属性 | 说明 |
|------|------|
| 触发条件 | 定位对象在某围栏区域内停留时间超过阈值 |
| 关联配置 | 围栏 + 滞留时间阈值 |
| 告警生成 | 对象在围栏内持续时间 > 阈值时触发 |
| 告警关闭 | 对象离开围栏区域后自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B{对象在围栏内?}
    B -->|是| C[累计在围栏内的停留时间]
    C --> D{停留时间 > 滞留阈值?}
    D -->|是| E{该对象已有<br>此围栏的滞留告警?}
    E -->|否| F[/🔔 生成滞留超时告警/]
    E -->|是| G([保持告警，不重复生成])
    D -->|否| H([继续计时，等待下次上报])
    B -->|否| I{该对象已有<br>此围栏的滞留告警?}
    I -->|是| J[/✅ 关闭滞留超时告警<br>重置计时器/]
    I -->|否| K[重置该围栏的停留计时]

    style F fill:#ff6b6b,color:#fff
    style J fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

### 6. 暂离超时

| 属性 | 说明 |
|------|------|
| 触发条件 | 定位对象离开指定围栏区域的时间超过阈值 |
| 关联配置 | 围栏 + 暂离时间阈值 |
| 告警生成 | 对象离开围栏持续时间 > 阈值时触发 |
| 告警关闭 | 对象回到围栏区域内后自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B{对象在围栏外?}
    B -->|是| C[累计离开围栏的时间]
    C --> D{离开时间 > 暂离阈值?}
    D -->|是| E{该对象已有<br>此围栏的暂离告警?}
    E -->|否| F[/🔔 生成暂离超时告警/]
    E -->|是| G([保持告警，不重复生成])
    D -->|否| H([继续计时，等待下次上报])
    B -->|否| I{该对象已有<br>此围栏的暂离告警?}
    I -->|是| J[/✅ 关闭暂离超时告警<br>重置计时器/]
    I -->|否| K[重置该围栏的暂离计时]

    style F fill:#ff6b6b,color:#fff
    style J fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

### 7. 手推车停滞超时

| 属性 | 说明 |
|------|------|
| 触发条件 | 手推车类定位对象在某位置静止超过阈值 |
| 关联配置 | 停滞时间阈值（30 分钟）+ 移动距离阈值（10 米） |
| 告警生成 | 手推车位置无变化持续时间 > 阈值时触发 |
| 告警关闭 | 手推车开始移动后自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B{对象类型是手推车?}
    B -->|否| Z([跳过，不适用此规则])
    B -->|是| C[对比与上次位置的距离]
    C --> D{位置是否发生移动?<br>距离 > 10米}
    D -->|否 静止| E[累计静止时间]
    E --> F{"静止时间 > 30分钟?"}
    F -->|是| G{已有停滞告警?}
    G -->|否| H[/🔔 生成手推车停滞超时告警/]
    G -->|是| I([保持告警，不重复生成])
    F -->|否| J([继续计时，等待下次上报])
    D -->|是 移动| K{已有停滞告警?}
    K -->|是| L[/✅ 关闭停滞超时告警<br>重置计时器/]
    K -->|否| M[重置静止计时]

    style H fill:#ff6b6b,color:#fff
    style L fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

## 状态类告警

### 8. 低电量

| 属性 | 说明 |
|------|------|
| 触发条件 | 定位标签上报电量低于设定阈值 |
| 关联配置 | 电量阈值（30%） |
| 告警生成 | 标签电量 < 阈值时触发 |
| 告警关闭 | 标签电量恢复到阈值以上 / 手动关闭 |

```mermaid
flowchart TD
    A([收到标签状态上报]) --> B[读取标签当前电量]
    B --> C{"电量 < 30%?"}
    C -->|是| D{该标签已有<br>低电量告警?}
    D -->|否| E[/🔔 生成低电量告警/]
    D -->|是| F([保持告警，不重复生成])
    C -->|否| G{该标签已有<br>低电量告警?}
    G -->|是| H[/✅ 关闭低电量告警<br>转入历史告警/]
    G -->|否| I([无事发生])

    style E fill:#ff6b6b,color:#fff
    style H fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

### 9. 聚集

| 属性 | 说明 |
|------|------|
| 触发条件 | 短时间内多个定位对象在小范围内聚集 |
| 关联配置 | 聚集半径 + 人数阈值（2人）+ 持续时间（10 分钟） |
| 告警生成 | 指定半径内对象数量 ≥ 2 且持续时间 ≥ 10 分钟时触发 |
| 告警关闭 | 聚集人数降至阈值以下后自动关闭 |

```mermaid
flowchart TD
    A([收到实时位置]) --> B[以每个对象为中心<br>计算指定半径内的对象数量]
    B --> C{"半径内对象数 ≥ 2?"}
    C -->|是| D[开始/继续计时]
    D --> E{"持续时间 ≥ 10分钟?"}
    E -->|是| F{已有聚集告警?}
    F -->|否| G[/🔔 生成聚集告警<br>记录聚集中心和人数/]
    F -->|是| H([保持告警，不重复生成])
    E -->|否| I([继续计时，等待下次上报])
    C -->|否| J{已有聚集告警?}
    J -->|是| K[/✅ 关闭聚集告警<br>重置计时器/]
    J -->|否| L[重置聚集计时]

    style G fill:#ff6b6b,color:#fff
    style K fill:#95e1d3,color:#333
    style A fill:#4ecdc4,color:#fff
```

---

## 告警生命周期

```mermaid
stateDiagram-v2
    [*] --> 监测中: 规则启用
    监测中 --> 告警触发: 满足触发条件
    告警触发 --> 当前告警: 写入告警记录
    当前告警 --> 当前告警: 持续满足条件（不重复生成）
    当前告警 --> 历史告警: 条件消除 / 手动关闭
    历史告警 --> [*]
    监测中 --> [*]: 规则禁用
```

---

## 关键设计要点

1. **去重机制** — 同一对象对同一规则，在告警未关闭前不重复生成新告警
2. **实时位置驱动** — 围栏类告警必须基于实时位置判断，不可根据历史位置生成
3. **自动关闭** — 大部分告警在条件消除后自动关闭，低电量可手动关闭
4. **作用范围过滤** — 围栏类告警（违规进入/离开）支持按标签/分组过滤，只对指定对象生效
5. **告警排队** — 大量告警同时触发时需排队处理，避免阻塞系统
6. **计时器管理** — 时间类告警（滞留/暂离/停滞）在条件消除后重置计时器
