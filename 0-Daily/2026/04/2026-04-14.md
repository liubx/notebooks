---
date: 2026-04-14
day_of_week: 星期二
tags:
  - daily
prev: "[[2026-04-13]]"
next: "[[2026-04-15]]"
---

# 2026-04-14 星期二

## 💼 工作

### 任务清单
```tasks
filter by function \
  const t=moment("2026-04-14"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/work
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")),y=task.tags.find(t=>t.startsWith("#type/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Work/"+p.replace("#project/","")+"/0-总览|🔍]]":y?"📂 "+y.replace("#type/","")+" [[2-Areas/Work/"+y.replace("#type/","")+"|🔍]]":"📌 待办"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
group by function \
  const s=task.tags.find(t=>t.startsWith("#status/")); \
  if(!s) return task.isDone?"":"⚪ 未开始"; \
  const m={"#status/doing":"🔵 进行中","#status/testing":"🟡 测试中","#status/waiting":"🟠 等待中","#status/done":"🟢 已完成"}; \
  return m[s]||s.replace("#status/","")
```

### 工作进展

- [[1-Projects/Work/广州机场/0-总览|广州机场]]：编写技术文档，完善项目汇报 PPT #project/广州机场
	- 编写告警规则说明文档，梳理 9 种告警类型的触发条件、关联配置和流程图
	- 整理综合定位与 TCDM 对接流程图，包括 MQ 消费流程和接口调用关系
	- 编写定位算法优化方案：电子围栏吸附（手推车停放区）和不可通行区飞点过滤
	- 完善项目汇报 PPT，添加建设期间优化处理章节（增补基站、楼层算法、飞点修正等）
	- 给 PPT 51 页添加演讲备注

### 会议记录

### 问题记录

## 📖 学习

### 任务清单
```tasks
filter by function \
  const t=moment("2026-04-14"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/study
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📌 待办"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 学习内容

### 技术笔记

### 阅读记录

## 🏠 个人

### 任务清单
```tasks
filter by function \
  const t=moment("2026-04-14"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/personal
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📌 待办"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 个人事项

### 健康记录

### 心情感悟
