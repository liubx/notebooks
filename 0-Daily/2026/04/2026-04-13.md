---
date: 2026-04-13
day_of_week: 星期一
tags:
  - daily
prev: "[[2026-04-12]]"
next: "[[2026-04-14]]"
---

# 2026-04-13 星期一

## 💼 工作

### 任务清单
```tasks
filter by function \
  const t=moment("2026-04-13"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/work
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")),y=task.tags.find(t=>t.startsWith("#type/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Work/"+p.replace("#project/","")+"/0-总览|🔍]]":y?"📂 "+y.replace("#type/","")+" [[2-Areas/Work/"+y.replace("#type/","")+"|🔍]]":"📌 待办"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
group by function \
  const s=task.tags.find(t=>t.startsWith("#status/")); \
  if(!s) return task.isDone?"":"⚪ 未开始"; \
  const m={"#status/doing":"🔵 进行中","#status/testing":"🟡 测试中","#status/waiting":"🟠 等待中","#status/done":"🟢 已完成"}; \
  return m[s]||s.replace("#status/","")
```

### 工作进展

- [[1-Projects/Work/广州机场/0-总览|广州机场]]：准备项目汇报 PPT 材料 #project/广州机场
	- 梳理项目建设概况：系统概述、技术原理、部署原则、建设数据、对接外部系统
	- 整理业务功能展示内容：实时地图、定位对象管理、电子围栏、告警功能详解
	- 整理手推车管理模块和委外人员管理模块内容
	- 准备建设期间优化处理案例：信号遮挡、轨迹跳变、镂空区错层等

### 会议记录

### 问题记录

## 📖 学习

### 任务清单
```tasks
filter by function \
  const t=moment("2026-04-13"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/study
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📌 待办"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 学习内容

### 技术笔记

### 阅读记录

## 🏠 个人

### 任务清单
```tasks
filter by function \
  const t=moment("2026-04-13"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/personal
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📌 待办"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 个人事项

### 健康记录

### 心情感悟
