---
date: 2026-03-17
day_of_week: 星期二
tags:
  - daily
prev: "[[2026-03-16]]"
next: "[[2026-03-18]]"
---

# 2026-03-17 星期二

## 💼 工作

### 任务清单
```tasks
filter by function \
  const t=moment("2026-03-17"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/work
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")),y=task.tags.find(t=>t.startsWith("#type/")); \
  return p?"📂 "+p.replace("#project/","")+" [[1-Projects/Work/"+p.replace("#project/","")+"/0-总览|🔍]]":y?"📂 "+y.replace("#type/","")+" [[2-Areas/Work/"+y.replace("#type/","")+"|🔍]]":"📌 临时"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 工作进展
- 麦钉项目所有任务完成：韩家村中间件部署、数据库备份恢复
- 平台 v2.7.0 升级完成，目前进入测试阶段

### 会议记录

### 问题记录

## 📖 学习

### 任务清单
```tasks
filter by function \
  const t=moment("2026-03-17"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/study
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"📂 "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📌 临时"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 学习内容

### 技术笔记

### 阅读记录

## 📝 笔记迁移

### 全部迁移完成
- Notion 迁移记录整理：补全所有 wikilinks 引用、删除空文件（Card/Test/出行清单/中间件更新）、重命名 Untitled 文件和图片
- Apple Notes 迁移记录整理：修复 wikilinks、移除别名
- Yuque 迁移记录整理：修复 `/` 分隔的伪路径引用、补全所有缺失的 wikilinks
- 清理 `Apple-Notes/` 临时源文件夹
- 三个来源（Apple Notes 1171个、Notion 80+、语雀 381个）全部迁移完成 ✅

## 🏠 个人

### 任务清单
```tasks
filter by function \
  const t=moment("2026-03-17"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/personal
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"📂 "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📌 临时"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 个人事项

### 健康记录

### 心情感悟
