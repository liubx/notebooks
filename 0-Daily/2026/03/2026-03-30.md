---
date: 2026-03-30
day_of_week: 星期一
tags:
  - daily
prev: "[[2026-03-29]]"
next: "[[2026-03-31]]"
---

# 2026-03-30 星期一

## 💼 工作

### 任务清单
```tasks
filter by function \
  const t=moment("2026-03-30"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/work
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")),y=task.tags.find(t=>t.startsWith("#type/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Work/"+p.replace("#project/","")+"/0-总览|🔍]]":y?"📂 "+y.replace("#type/","")+" [[2-Areas/Work/"+y.replace("#type/","")+"|🔍]]":"📂 临时"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 工作进展

#### 平台更新 v2.7 上线升级

- [ ] 完成 v2.7 上线升级 #task/work #project/平台更新v2-7

#### 广州机场 - 手推车管理页面联调

- [x] 网页版手推车管理页面联调 #task/work #project/广州机场 ✅ 2026-03-31

#### 上港仓储

- 现场测试入库流程，发现以下问题：
  - 标签在未到库位时检测不到，导致提早入库（038001001098、038001001779）
  - 已入库货物被莫名拉出库（038001001804）
  - 货物标签 038001001372 检测不到，已替换新标签
- 已同步以上问题到飞书任务清单
- 创建验证任务分配给刘仁跃，验证 3/29 的 App 改动

### 会议记录

### 问题记录

## 📖 学习

### 任务清单
```tasks
filter by function \
  const t=moment("2026-03-30"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/study
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📂 临时"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 学习内容

### 技术笔记

### 阅读记录

## 🏠 个人

### 任务清单
```tasks
filter by function \
  const t=moment("2026-03-30"),d='day',{due:{moment:u},start:{moment:s},created:{moment:c},done:{moment:D}}=task,y=moment(t).subtract(1,d),b=m=>m?.isSameOrBefore(t,d),q=m=>m?.isSame(t,d); \
  return !!(!task.isDone?!u&&b(s)||b(u)||b(s)||q(c):q(D)||D?.isSame(y,d));
path does not include 4-Archives
tags include #task/personal
hide toolbar
hide task count
hide created date
hide start date
hide done date
hide tags
hide backlink
hide edit button
hide postpone button
sort by priority
sort by due
sort by start
group by function \
  const p=task.tags.find(t=>t.startsWith("#project/")); \
  return p?"⭐ "+p.replace("#project/","")+" [[1-Projects/Personal/"+p.replace("#project/","")+"/0-总览|🔍]]":"📂 临时"
group by function \
  const h=task.heading,f=task.file.path.replace(/\.md$/,""); \
  return h?"📂 "+h+" [["+f+"#"+h+"|📋]]":""
```

### 个人事项

### 健康记录

### 心情感悟
