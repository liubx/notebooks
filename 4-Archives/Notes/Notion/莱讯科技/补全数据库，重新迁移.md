# 补全数据库，重新迁移

- [ ]  检查所有带geom的表，修正geom字段类型
    
    t_project （config里面，这需要手动更新）
    
    位置服务
    
    t_position(PointZ) / t_fence(Polygon) / t_alarm(PointZ)
    
    设备服务
    
    t_anchor(PointZ) / t_camera(PointZ)
    
    地图服务
    
    t_space(Polygon) / t_floor(Polygon)  / t_polygon(PolygonZ) / t_poi(Point)  / t_scenegraph(Point)
    
- [ ]  补充deleted字段
- [ ]  补充t_polygon 的 PolygonZ 问题
- [ ]  确认create_schema 里的遇见一致
- [ ]  补充创建基础基站/标签类型的sql
- [ ]  操作完后重新迁移management/position/map/device 四个数据库
- [ ]  添加notification 的数据表
- [ ]  修复超级表循环问题