# 禅道问题处理

## 登录数据库

```bash
docker exec -it zentao bash

mysql -u root -p

4wwgloYnJKbOx9aRiZD5
```

A：如何远程连接容器里的mysqlQ：请参考：https://www.zentao.net/book/zentaopmshelp/276.html

1)、确认主机与容器中mysql有端口映射；2)、确认/etc/mysql/mariadb.conf.d/50-server.cnf 中的“ bind-address = 0.0.0.0”；3)、确认远程登录用户权限，如使用root用户。

```
SELECT user, host FROM mysql.user;

CREATE USER 'zentao'@'%' IDENTIFIED BY '4wwgloYnJKbOx9aRiZD5';

GRANT ALL PRIVILEGES ON *.* TO 'zentao'@'%' IDENTIFIED BY '4wwgloYnJKbOx9aRiZD5';

flush privileges;

```

保存状态的表 zt_storystage